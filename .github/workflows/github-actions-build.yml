name: BuildMap
on:
  push:
    branches: [ master ]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd ${{ github.workspace }}
      - shell: pwsh
        run: ./Generate-Map.ps1
      - run: |
          mkdir maps
          cp mymap.svg maps/
          cp mymapus.svg maps/
      - name: Install Inkscape
        run: sudo apt-get install -y inkscape
      - name: Convert to rasters
        run: |
          inkscape -z -e maps/mymap.png -w 4960 -y 0 mymap.svg
          inkscape -z -e maps/mymapthb.png -w 800 -y 0 mymap.svg
          inkscape -z -e maps/mymapus.png -w 4960 -y 0 mymapus.svg
          inkscape -z -e maps/mymapusthb.png -w 800 -y 0 mymapus.svg
      - uses: actions/upload-artifact@v2
        with: 
          name: maps
          path: maps